<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 예제 6-7. 로그 레벨을 DEBUG로 조정. -->
    <!-- 테스트를 쉽게 할 수 있도록 logback.xml 구성 파일을 수정해 모든 클래스의 로그 레벨을 DEBUG로 지정.
         DEBUG 레벨에서는 스프링 JDBC 모듈이 데이터베이스로 전달하는 모든 SQL을 출력하므로 무슨일이 일어나는지 정확히 알 수 있다.-->
    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resultJUL>true</resultJUL>
    </contextListener>

    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{5} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.apress.prospring5" level="debug" />

    <logger name="org.springframework" level="off" />

    <root level ="debug">
        <appender-ref ref="console" />
    </root>

</configuration>